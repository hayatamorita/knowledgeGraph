# Optional. If unset, the app uses a local rule-based extractor for development.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Current MVP persists to data/store.json.
# The SQL schema for future Supabase/PostgreSQL migration is in supabase/migrations.
